'use strict';
// =============================================================================
// TICKES CONTROLLER
// =============================================================================
angular
    .module( 'track.tickets' )
    .controller( 'tickets.indexCtrl' , ticketsIndexCtrl );

ticketsIndexCtrl.$inject = [
   '$scope'
];
/* jshint -W101 */
function ticketsIndexCtrl( $scope ) {
/* jshint +W101 */
   
}
